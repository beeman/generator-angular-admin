language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-admin' ]; then cd .. && eval "mv $currentfolder generator-angular-admin" && cd generator-angular-admin; fi

